{
  "name": "schaufel-monorepo",
  "version": "0.14.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "version:patch": "npm version patch --no-git-tag-version && npm version patch --workspaces",
    "version:minor": "npm version minor --no-git-tag-version && npm version minor --workspaces",
    "push-version": "./scripts/postversion.sh $npm_package_version"
  },
  "devDependencies": {
    "prettier": "latest",
    "turbo": "latest"
  }
}
